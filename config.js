/* Nambarone pre-launch configuration.
   Payments are intentionally OFF until Cashfree KYC + production payment wiring are complete.
   If you use JSONBin during pre-launch, keep it READ-ONLY from the browser. Never put a write/master key in frontend code. */
window.NAMBARONE = {
  site: "https://nambarone.lol",
  launchMode: true,
  paymentsLive: false,
  jsonbinId: "",
  jsonbinKey: ""
};
